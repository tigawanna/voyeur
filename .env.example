TMDB_API_KEY=your_tmdb_api_key_here
VITE_APP_URL=http://localhost:3072
# Auth bypass is server-only. Set BYPASS_AUTH=true in .dev.vars (local) or Cloudflare Worker variables (production).
# VITE_BYPASS_AUTH=true still works locally as a fallback for the dev server process env.
